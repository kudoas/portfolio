{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec"
    // "types": ["vitest/globals"] <- なぜか spec で型エラーになるので、tsconfig.json で設定している
  },
  "files": ["src/setup-vitest.ts"],
  "include": ["src/**/*.spec.ts", "src/**/*.d.ts"]
}
